import * as messages from "./messages";
import * as embeds from "./embeds";
import * as commands from "./commands";

export default {
  messages: messages,
  embeds: embeds,
  commands: commands,
};
